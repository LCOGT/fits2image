fits-utils - Shared library for converting fits files to jpeg, scaling them, etc

The fits-utils package provides common libraries for the frame_database and telops

v 0.1.0 - Initial version with conversion code from frame_database